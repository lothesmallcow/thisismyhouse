{
  "name": "bubbles-calendar",
  "version": "1.0.0",
  "description": "Auto-placing scheduler: fixed events + flexible bubbles solved into the gaps between them.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "npm run build && node server.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
